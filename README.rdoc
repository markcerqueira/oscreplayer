== osc[recorder | player] - <em>a utility to record and replay OSC messages</em>
originally by Tom Lieber with modifications by Mark Cerqueira, Spencer Salazar

===HOW TO
* *bundle* *install* to get the required gems

   # listens on the port specified and encodes content into the specified file
   ruby oscrecorder.rb $PORT_NUMBER > $FILE.TXT